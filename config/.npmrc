# NPM Configuration for Universal Development Environment
# This file supports both public and enterprise registry configurations

# Default registry (will be overridden by environment variables)
registry=https://registry.npmjs.org/

# Enterprise proxy settings (uncomment if using corporate proxy)
# proxy=http://proxy.company.com:8080
# https-proxy=http://proxy.company.com:8080

# SSL Configuration for enterprise environments
# strict-ssl=true
# ca=/certs/ca-bundle.crt

# Cache configuration
cache=/cache/npm
tmp=/cache/npm-tmp

# Package installation preferences
save-exact=true
package-lock=true
audit-level=moderate

# Timeout settings (helpful for slow enterprise networks)
timeout=60000
network-timeout=300000

# Performance optimizations
prefer-offline=true
prefer-dedupe=true

# Security settings
fund=false
audit=true

# Enterprise registry authentication (use environment variables)
# //artifactory.company.com/artifactory/api/npm/npm-virtual/:_authToken=${NPM_TOKEN}
# //artifactory.company.com/artifactory/api/npm/npm-virtual/:always-auth=true

# Scoped packages for enterprise environments
# @company:registry=https://artifactory.company.com/artifactory/api/npm/npm-virtual/
# @internal:registry=https://artifactory.company.com/artifactory/api/npm/npm-virtual/

# Logging level
loglevel=warn

# Node.js compatibility
engine-strict=false
save-prefix=^

# Development flags
ignore-scripts=false
optional=true
production=false